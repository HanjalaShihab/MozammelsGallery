function showSearch() {
    const search = document.querySelector(".form-control");
        search.style.width = "180px";
        search.style.visibility = "visible";
        search.focus(); // Optionally, focus the input for immediate typing
}